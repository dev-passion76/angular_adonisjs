import { Routes } from '@angular/router';
import { FormTestAdonisComponent } from './components/form-test-adonis/form-test-adonis.component';

export const routes: Routes = [
    {
      path: '',component: FormTestAdonisComponent,
    }
  ];
